extends ../layout

block content 
  form(action="/lead/"+lead.id+"/edit", method="POST")
    input(type="email", name="lead_email" value=lead.email placeholder="Enter your email address")
    button(type="submit") Save  